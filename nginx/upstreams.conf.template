upstream miniapp_api {
    server api:8001;
}

upstream gracehub_backend {
    server master-bot:9443;
    # Если нужно добавить user-worker инстансы, добавьте здесь, например:
    # server gracehub-worker-some-id:port;
}
